# File size and folder structure

The max file size is 3000 words. Words are counted with the `wc -w {FILE}` command.
Files above this limit must be split.

Think about the folder/file tree as an easy search index to find knowledge. If everything would be in one file, things would be hard to find. A folder/file tree helps organize knowledge
so the person after you can find it again easily.