# Archiving & Versioning

When any files in `dossier/` is no longer actively needed anymore, it should be moved into `dossier/archive`. Add a note at the top of the archived file: date archived and what replaced it. The new protocol should briefly reference what it replaced and why. The goal is to keep the dossier clean