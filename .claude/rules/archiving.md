# Archiving & Versioning

When any files in `patient_file/` is no longer actively needed anymore, it should be moved into `patient_file/archive`. Add a note at the top of the archived file: date archived and what replaced it. The new protocol should briefly reference what it replaced and why. The goal is to keep the patient_file clean